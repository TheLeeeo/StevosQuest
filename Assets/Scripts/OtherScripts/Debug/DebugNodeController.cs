using UnityEngine;

public class DebugNodeController : MonoBehaviour
{
    /*public Vector2[] ConnectedNodes = new Vector2[WorldGrid.MaxNumberOfConnectedNodes];

    private int NumberOfConnectedNodes = 0;

    public void AddConnectedNodePosition(Vector2 RelativeNodePosition)
    {
        ConnectedNodes[NumberOfConnectedNodes] = RelativeNodePosition;

        NumberOfConnectedNodes++;
    }


    public void Update()
    {
        for (int i = 0; i < NumberOfConnectedNodes; i++)
        {
            Debug.DrawLine(transform.position, new Vector3(ConnectedNodes[i].x, ConnectedNodes[i].y, 0), Color.yellow);
        }
    }*/
}
